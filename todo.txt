Tests for data
 - Write a test with 4th day data that does 3rd round calculation correctly
 - Abstract player data so we get consistent results
 - Write tests for that


 HomeController

 - leaderboard = LB.new
 - scores = lb.scores_for_user(user) (returns new wrapped scores)
 - my_total = Player.calculate_total(scores)